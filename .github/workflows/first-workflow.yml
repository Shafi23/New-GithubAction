name: My first work flow.

on: push

jobs:
    hello:
        runs-on: ubuntu-latest

        steps:
            - name: Print Message.
            run: echo "Hello from github action"
        
